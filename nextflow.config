params {
    input_dir = "data"
    output_dir = "results"

    # Filter parameters
    low_freq = 1.0
    high_freq = 40.0

    # EEG channel configuration
    pick_channel = "C3"
}

env {
    PATH = "${baseDir}/bin:${System.getenv('PATH')}"
}
